# === MongoDB Cloud ===
spring.data.mongodb.uri=mongodb+srv://Aadil:MdAadil786@cluster0.txredhm.mongodb.net/snakeDB?retryWrites=true&w=majority
spring.data.mongodb.database=snakeDB

# === Web Settings ===
server.port=8080
spring.application.name=snake-game

# === Logging (Optional Debugging) ===
logging.level.org.springframework=INFO
logging.level.com.aadil=DEBUG

# === Thymeleaf (Optional - Remove if not using) ===
spring.thymeleaf.enabled=false

# === CORS (Optional - if frontend is separate) ===
# spring.web.cors.allowed-origins=http://localhost:5500
