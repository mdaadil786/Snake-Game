# === MongoDB Cloud ===
spring.data.mongodb.uri=mongodb+srv://<userName>:<password>cluster0.txredhm.mongodb.net/databasename?retryWrites=true&w=majority
spring.data.mongodb.database=snakeDB

# === Web Settings ===
server.port=portNum
spring.application.name=snake-game

# === Logging (Optional Debugging) ===
logging.level.org.springframework=INFO
logging.level.com.aadil=DEBUG

# === Thymeleaf (Optional - Remove if not using) ===
spring.thymeleaf.enabled=false

